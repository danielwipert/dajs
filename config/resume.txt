# Placeholder — paste Daniel's resume here as clean plain text before Phase 4.
# This file is loaded once at pipeline start and passed verbatim to every
# scoring prompt as fixed context. No formatting tricks; LLMs read prose fine.
